import { type User } from "next-auth"



const DeliveryForm = ({prefetchedUser}:{prefetchedUser?: User}) => {
  return (
    <div className="flex flex-col gap-4 mt-4 h-full">
     
    </div>
  )
}

export { DeliveryForm }
